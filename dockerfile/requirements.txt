# Training / inference / serving stack for the KSA reference image.
# Versions pinned for reproducibility.
#
# Packages intentionally excluded from this file:
#   - torch / torchvision / flash-attn / summary_attn / flash_attn_cute :
#     installed explicitly in the Dockerfile (summary_attn and flash_attn_cute
#     are bundled as wheels under summary_attention_kernel/).
#   - apex / deep_ep / transformer_engine / sglang / sgl-kernel / mamba-ssm /
#     causal_conv1d / pytorch-triton and any site-specific data/runtime
#     clients : no stable public wheel at the exact pinned version. Install
#     from source only if your workload needs them.
#   - pip-resolved transitive dependencies are intentionally left unpinned here;
#     pip will pick compatible versions from the pins below.

# ---- Core training / inference ----
transformers==4.57.1
tokenizers==0.22.1
accelerate==1.9.0
safetensors==0.5.3
sentencepiece==0.2.0
datasets==4.0.0
einops==0.8.1
numpy==2.2.6
pandas==2.3.1
pyarrow==20.0.0
tqdm==4.67.1
rich==14.0.0
huggingface-hub==0.34.4
hf_transfer==0.1.9
modelscope==1.28.0
blobfile==3.0.0
tiktoken==0.9.0

# ---- Config / logging ----
hydra-core==1.3.2
omegaconf==2.3.0
wandb==0.21.0
tensorboard==2.19.0
python-dotenv==1.1.1

# ---- Large-model training stack ----
megatron-core==0.12.1
verl==0.4.1
peft==0.16.0
torchao==0.9.0
torchdata==0.11.0
tensordict==0.6.2
torch_memory_saver==0.0.8
compressed-tensors==0.10.2
nvidia-modelopt==0.33.0

# ---- Inference / linear / sparse kernels ----
flashinfer-python==0.2.5
flash-linear-attention==0.3.2
xgrammar==0.1.19
outlines==0.1.11
quack-kernels==0.2.4

# ---- Orchestration ----
ray==2.47.1

# ---- Serving (RL rollout, eval servers) ----
fastapi==0.116.1
uvicorn==0.35.0
starlette==0.47.1
openai==1.97.0
litellm==1.74.3

# ---- Runtime utilities ----
msgspec==0.19.0
codetiming==1.4.0
jsonschema==4.24.0
pydantic==2.11.7
scipy==1.16.0
nvidia-ml-py==12.575.51
nvitop==1.6.2
py-spy==0.4.0
torch-tb-profiler==0.4.3

# ---- Dev / test ----
pytest==8.4.1
pytest-cov==6.2.1
pytest-mock==3.14.1
pytest-random-order==1.2.0
